'use strict';

/**
 * Configuration.
 *
 * Warning: do not add functions, only variables.
 */
var Config = {
  WEB_SERVICE_HOSTNAME: localStorage.getItem('WEB_SERVICE_HOSTNAME') || 'http://localhost:3000'
};
